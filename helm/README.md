# Helm — `helm/`

`helm/sentinelops-demo/` — the chart that deploys the 3 demo services + their
Postgres into the `sentinelops-demo` namespace, with the NodePorts, resource
limits, and readiness/liveness probes the failure scenarios depend on.

> Chart authored in Phase 2.
